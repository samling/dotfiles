################
### KEYWORDS ###
################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Terminal
$terminal = wezterm

# File manager
$fileManager = thunar

# Launcher
$launcher = rofi -show drun
#$launcher = fuzzel --output=$(hyprctl monitors -j | jq -r '.[] | select(.focused) | .name')

# Copy Menu
$copyMenu = wezterm -e --class clipse 'clipse'

# Hyprlock
$hyprlock = hyprlock
