## Environment
- `grep` is aliased to `rg` (ripgrep) on this system.
- `rm` is aliased to `rm -i` — use `"rm"` to bypass the confirmation prompt.
- `mv` is aliased to `mv -i` — use `"mv"` to bypass the confirmation prompt.
