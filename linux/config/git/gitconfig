[core]
        pager = delta

[pager]
        diff = delta
        log = delta
        reflog = delta
        show = delta

[interactive]
        diffFilter = delta --color-only

[include]
        path = ~/dotfiles/linux/config/delta/catppuccin.gitconfig

[delta]
        navigate = true
        features = catppuccin-macchiato side-by-side line-numbers decorations
        whitespace-error-style = 22 reverse

[merge]
        conflictstyle = diff3

[diff]
        colorMoved = default

[user]
        email = samlingx@gmail.com
        name = Sam Boynton

[includeIf "gitdir:~/Documents/NVIDIA/"]
        path = ~/nv-dotfiles/linux/config/git/gitconfig
[init]
	defaultBranch = main
[credential]
	helper = store
