// Window title configuration
export const windowTitle = {
    // Custom window title mappings
    // Format: [class_regex, icon, display_name]
    title_map: [
        // Add your custom mappings here
        // Example: ['firefox-developer-edition', '󰈹', 'Firefox Dev Edition'],
    ],
    
    // Whether to use custom titles from the mapping
    useCustomTitle: false,
    
    // Whether to use class name instead of window title
    useClassName: false,
    
    // Maximum length for window titles (0 for no limit)
    maxTitleLength: 50,
}; 